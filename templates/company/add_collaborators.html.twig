{% extends "company_dashboard.html.twig" %}

{% block body %}
    <div class="content-body">
        <div class="container-fluid">
            {{ include('partials/_page_title.html.twig', {'titles': {'Mes Entreprises': path('app_company_by_company_profile_index'), (company.name): path('app_company_show', {'id': company.id}), 'Ajouter collaborateurs': ''}}) }}
            <!-- row -->
            <div class="row">
                <div class="col-xl-6 col-xxl-12">
                    <div class="card">
                        <div class="card-header">
                            <h4 class="card-title">Ajouter des collaborateurs</h4>
                        </div>
                        <div class="card-body">
                            <div class="basic-form">
                                {{ form_start(form) }}
                                    <div class="row">
                                        <div class="col-sm-6">
                                            {{ form_label(form.collaborators, 'Collaborateurs', {'label_attr': {'class': 'form-label'}}) }}  
                                            {{ form_widget(form.collaborators, {'attr': {'class': 'disabling-options'}}) }}
                                        </div>
                                    </div>
                                    <button type="submit" class="btn btn-primary mt-3">{{ submit_label ?? 'Enregistrer' }}</button>
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- #/ row --> 
        </div>
    </div>
{% endblock %}
