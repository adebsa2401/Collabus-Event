<!DOCTYPE html>

<title>EventParticipationRequest</title>

{% block body %}
    <h1>EventParticipationRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_participation_request.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ event_participation_request.status }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ event_participation_request.createdAt ? event_participation_request.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_event_participation_request_index') }}">back to list</a>

    <a href="{{ path('app_event_participation_request_edit', {'id': event_participation_request.id}) }}">edit</a>

    {{ include('event_participation_request/_delete_form.html.twig') }}
{% endblock %}
