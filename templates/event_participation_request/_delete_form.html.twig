<form method="post" action="{{ path('app_event_participation_request_delete', {'id': event_participation_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_participation_request.id) }}">
    <button class="btn">Delete</button>
</form>
