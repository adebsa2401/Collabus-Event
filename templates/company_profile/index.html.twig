{% extends "admin_dashboard.html.twig" %}

{% block body %}
	<div class="content-body">
		<div class="container-fluid">
			{{ include('partials/_page_title.html.twig', {'titles': {'Utilisateurs': '', 'Entreprises': ''}}) }}
			<!-- row -->

			<div class="row">
				<div class="col-12">
					<div class="card">
						<div class="card-header">
							<h4 class="card-title">Entreprises</h4>
						</div>
						<div class="card-body">
							<div class="table-responsive">
								<table id="example" class="display" style="min-width: 845px">
									{% if company_profiles|length > 0 %}
										<thead>
											<tr>
												<th>#</th>
												<th>Nom</th>
												<th>Email</th>
												<th>Entreprises</th>
												<th>Verifié</th>
												<th>actions</th>
											</tr>
										</thead>
										<tbody>
											{% for company_profile in company_profiles %}
												<tr>
													<td>{{ loop.index }}</td>
													<td>{{ company_profile.name }}</td>
													<td>{{ company_profile.user.email }}</td>
													<td>{{ company_profile.companies|length }}</td>
													<td>{{ company_profile.user.isVerified ? 'Oui' : 'Non' }}</td>
													<td>
														<a href="{{ path('app_company_profile_show', {'id': company_profile.id}) }}" data-toggle="tooltip" data-placement="top" title="Consulter">
															<i class="fa fa-eye text-info"></i>
														</a>
													</td>
												</tr>
											{% endfor %}
										</tbody>
									{% else %}
										Aucun compte entreprise n'a été trouvée
									{% endif %}
								</table>
							</div>
						</div>
					</div>
				</div>
			</div>
			<!-- #/ row -->
		</div>
	</div>
{% endblock %}

{% block javascripts %}
<script>
	$('#confirmButton').click(function () {
window.location.href = $(this).attr('href');
});
</script {% endblock %}
